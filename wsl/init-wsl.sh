#!/usr/bin/env bash

# if [[ -z $TMUX && -n $PS1 ]]; then
#   function tmux() {
#     if [[ $# == 0 ]] && tmux has-session 2>/dev/null; then
#       exec tmux attach-session
#     else
#       exec tmux "$@"
#     fi
#   }
# fi
